name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  detect-changes:
    name: Detect changed paths
    runs-on: ubuntu-latest
    outputs:
      tls: ${{ steps.filter.outputs.tls }}
      docs: ${{ steps.filter.outputs.docs }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4.0.2
        id: filter
        with:
          filters: |
            tls:
              - 'pycubrid/aio/**'
              - 'pycubrid/connection.py'
              - 'pycubrid/_connection_common.py'
              - '.github/workflows/**'
            docs:
              - '**/*.md'
              - 'docs/**'

  doc-lint:
    name: Documentation lint
    needs: detect-changes
    if: needs.detect-changes.outputs.docs == 'true'
    uses: cubrid-lab/.github/.github/workflows/doc-lint.yml@main
    with:
      exclude: "node_modules/,vendor/"
      fail-on-issue: false  # advisory mode during initial rollout


  compat-check:
    name: Public API compatibility
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6
        with:
          python-version: "3.12"
      - name: Install pycubrid (import-only)
        run: |
          python -m pip install --upgrade pip
          pip install -e .
      - name: Verify public API surface matches api-baseline.json
        run: python scripts/check_public_api.py

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6
        with:
          python-version: "3.12"
      - name: Install pinned ruff from pyproject.toml
        run: pip install -e ".[dev]"
      - name: Ruff check
        run: ruff check pycubrid/ tests/
      - name: Ruff format check
        run: ruff format --check pycubrid/ tests/

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Mypy strict
        run: mypy pycubrid/ --strict

  offline-tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pytest-cov
      - name: Run offline tests with coverage
        run: |
          python -m pytest tests/ \
            --ignore=tests/test_integration.py \
            --ignore=tests/test_parity_integration.py \
            -v --tb=short \
            --cov=pycubrid \
            --cov-report=term-missing \
            --cov-report=xml \
            --cov-fail-under=95
      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.10' && github.actor != 'dependabot[bot]'
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          verbose: true
          fail_ci_if_error: false
        env:
          CODECOV_RETRIES: 3

  packaging-smoke-test:
    runs-on: ubuntu-latest
    needs: [offline-tests]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6
        with:
          python-version: "3.12"
      - name: Install build tool
        run: |
          python -m pip install --upgrade pip
          pip install build
      - name: Build sdist and wheel
        run: python -m build
      - name: Smoke test wheel install
        run: |
          python -m venv /tmp/test-wheel
          /tmp/test-wheel/bin/pip install dist/*.whl
          cd /tmp
          /tmp/test-wheel/bin/python -c "import pycubrid; print(pycubrid.__version__)"
          /tmp/test-wheel/bin/python -c "import pycubrid.aio; print('async OK')"
          /tmp/test-wheel/bin/python -c "from pycubrid import connect, Date, Time, Timestamp, Binary; print('public API OK')"
      - name: Smoke test sdist install
        run: |
          python -m venv /tmp/test-sdist
          /tmp/test-sdist/bin/pip install dist/*.tar.gz
          cd /tmp
          /tmp/test-sdist/bin/python -c "import pycubrid; print(pycubrid.__version__)"

  integration-tests:
    runs-on: ubuntu-latest
    needs: [lint, typecheck, offline-tests, packaging-smoke-test]
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.14"]
        cubrid-version: ["10.2", "11.0", "11.2", "11.4"]
    services:
      cubrid:
        image: cubrid/cubrid:${{ matrix.cubrid-version }}
        env:
          CUBRID_DB: testdb
        ports:
          - 33000:33000
        options: >-
          --health-cmd "csql -u dba testdb -c 'SELECT 1'"
          --health-interval 15s
          --health-timeout 10s
          --health-retries 10
          --health-start-period 30s
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install pycubrid
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Wait for CUBRID readiness
        run: |
          for i in $(seq 1 30); do
            if python -c "
          from pycubrid import connect
          try:
              conn = connect('CUBRID:localhost:33000:testdb:::', user='dba', password='')
              cur = conn.cursor()
              cur.execute('SELECT 1')
              cur.close()
              conn.close()
              print('CUBRID ready')
              exit(0)
          except Exception as e:
              print(f'Not ready: {e}')
              exit(1)
          "; then
              break
            fi
            echo "Waiting for CUBRID... (attempt $i/30)"
            sleep 5
          done
      - name: Run integration tests
        env:
          CUBRID_TEST_URL: "cubrid://dba@localhost:33000/testdb"
        run: |
          python -m pytest tests/test_*integration*.py -v --tb=short

  integration-tls:
    # Per-PR TLS lane (issue #159) — gated on changes to TLS-relevant code
    # paths. Mirrors the integration-tls job in integration-full.yml but with
    # a single anchored matrix entry (3.14 × 11.4) to keep per-PR cost low
    # while still catching regressions BEFORE merge. integration-full.yml
    # remains the source of truth for the full TLS matrix.
    needs: [detect-changes, lint, typecheck, offline-tests, packaging-smoke-test]
    if: needs.detect-changes.outputs.tls == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.14"]
        cubrid-version: ["11.4"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install pycubrid
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
      - name: Start CUBRID broker with SSL=ON
        run: |
          set -euxo pipefail
          mkdir -p "$RUNNER_TEMP/tls"
          openssl req -x509 -newkey rsa:2048 -nodes -days 1 \
            -keyout "$RUNNER_TEMP/tls/cas_ssl_cert.key" \
            -out "$RUNNER_TEMP/tls/cas_ssl_cert.crt" \
            -subj "/CN=localhost" \
            -addext "subjectAltName=DNS:localhost"
          chmod 600 "$RUNNER_TEMP/tls/cas_ssl_cert.key"
          docker run -d --rm --name cubrid-tls \
            -e CUBRID_DB=testdb \
            -p 33000:33000 \
            cubrid/cubrid:${{ matrix.cubrid-version }}
          for i in $(seq 1 40); do
            if docker exec cubrid-tls bash -lc "cubrid broker status BROKER1 >/dev/null 2>&1"; then
              echo "BROKER1 reachable"
              break
            fi
            echo "Waiting for BROKER1... (attempt $i/40)"
            sleep 3
          done
          docker cp "$RUNNER_TEMP/tls/cas_ssl_cert.crt" cubrid-tls:/home/cubrid/CUBRID/conf/cas_ssl_cert.crt
          docker cp "$RUNNER_TEMP/tls/cas_ssl_cert.key" cubrid-tls:/home/cubrid/CUBRID/conf/cas_ssl_cert.key
          docker exec cubrid-tls bash -lc "chown cubrid:cubrid /home/cubrid/CUBRID/conf/cas_ssl_cert.* && chmod 600 /home/cubrid/CUBRID/conf/cas_ssl_cert.key"
          docker exec cubrid-tls bash -lc "
            set -eux
            conf=\$CUBRID/conf/cubrid_broker.conf
            sed -i '/^\[%BROKER1\]/,/^\[/ s/^\s*SSL\s*=\s*OFF/SSL=ON/' \$conf
            grep -A2 '^\[%BROKER1\]' \$conf | head -20
            cubrid broker restart
            cubrid server start testdb || cubrid server start cubdb || true
          "
          cp "$RUNNER_TEMP/tls/cas_ssl_cert.crt" "$RUNNER_TEMP/cas_ssl_cert.crt"
          ls -l "$RUNNER_TEMP/cas_ssl_cert.crt"
      - name: Probe TLS broker readiness
        env:
          CUBRID_TLS_TEST_CA_FILE: ${{ runner.temp }}/cas_ssl_cert.crt
        run: |
          set -euxo pipefail
          for i in $(seq 1 30); do
            if python - <<'PY'
          import os, ssl, sys
          import pycubrid
          ctx = ssl.create_default_context(cafile=os.environ["CUBRID_TLS_TEST_CA_FILE"])
          try:
              conn = pycubrid.connect(
                  host="localhost", port=33000,
                  database="testdb", user="dba", password="",
                  ssl=ctx, connect_timeout=5,
              )
              cur = conn.cursor(); cur.execute("SELECT 1"); cur.fetchone(); cur.close(); conn.close()
              print("TLS broker ready")
              sys.exit(0)
          except Exception as exc:
              print(f"Not ready yet: {exc}")
              sys.exit(1)
          PY
            then
              break
            fi
            echo "Waiting for TLS broker... (attempt $i/30)"
            sleep 3
          done
          python - <<'PY'
          import os, ssl, pycubrid
          ctx = ssl.create_default_context(cafile=os.environ["CUBRID_TLS_TEST_CA_FILE"])
          conn = pycubrid.connect(host="localhost", port=33000, database="testdb",
                                   user="dba", password="", ssl=ctx)
          conn.close()
          print("Final TLS probe OK")
          PY
      - name: Run async TLS integration tests
        timeout-minutes: 5
        env:
          CUBRID_TEST_URL: "cubrid://dba@localhost:33000/testdb"
          CUBRID_TEST_HOST: localhost
          CUBRID_TEST_PORT: "33000"
          CUBRID_TEST_DB: testdb
          CUBRID_TEST_USER: dba
          CUBRID_TEST_PASSWORD: ""
          CUBRID_TLS_TEST_HOST: localhost
          CUBRID_TLS_TEST_PORT: "33000"
          CUBRID_TLS_TEST_DB: testdb
          CUBRID_TLS_TEST_USER: dba
          CUBRID_TLS_TEST_PASSWORD: ""
          CUBRID_TLS_TEST_CA_FILE: ${{ runner.temp }}/cas_ssl_cert.crt
          CUBRID_TLS_TEST_MISMATCH_HOST: "127.0.0.1"
          SSL_CERT_FILE: ${{ runner.temp }}/cas_ssl_cert.crt
        run: |
          set -euxo pipefail
          python -m pytest tests/test_aio_ssl_integration.py -v --tb=short \
            -o addopts="" -p no:cacheprovider \
            --no-header --no-summary -rN 2>&1 | tee tls-run.log
          unexpected_skips=$(grep -E " SKIPPED " tls-run.log \
            | grep -v "tests/test_aio_ssl_integration.py::test_aio_ssl_handshake_failure" || true)
          if [ -n "$unexpected_skips" ]; then
            echo "::error::TLS integration tests skipped unexpectedly — broker provisioning is incomplete"
            echo "$unexpected_skips"
            exit 1
          fi
      - name: Stop CUBRID TLS broker
        if: always()
        run: docker stop cubrid-tls || true

  ci-gate:
    name: CI Gate (all checks must pass)
    if: always()
    needs: [lint, typecheck, offline-tests, packaging-smoke-test, integration-tests, compat-check, integration-tls]
    runs-on: ubuntu-latest
    steps:
      - name: Check job results
        env:
          R_LINT: ${{ needs.lint.result }}
          R_TYPECHECK: ${{ needs.typecheck.result }}
          R_OFFLINE: ${{ needs.offline-tests.result }}
          R_PACKAGING: ${{ needs.packaging-smoke-test.result }}
          R_INTEGRATION: ${{ needs.integration-tests.result }}
          R_COMPAT: ${{ needs.compat-check.result }}
          R_TLS: ${{ needs.integration-tls.result }}
        run: |
          set -euo pipefail
          fail=0
          for var in R_LINT R_TYPECHECK R_OFFLINE R_PACKAGING R_INTEGRATION R_COMPAT; do
            if [[ "${!var}" != "success" ]]; then
              echo "::error::Required job ${var#R_} result: ${!var}"
              fail=1
            fi
          done
          # integration-tls is path-gated (issue #159); `skipped` is acceptable
          # when no TLS-relevant files changed, but any other non-success state
          # (failure, cancelled) must fail the gate.
          if [[ "$R_TLS" != "success" && "$R_TLS" != "skipped" ]]; then
            echo "::error::integration-tls result: $R_TLS"
            fail=1
          fi
          if [[ "$fail" -ne 0 ]]; then
            exit 1
          fi
          echo 'All CI checks passed ✓'
